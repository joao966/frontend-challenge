export type themeType = {
  colors: {
      primary: string;
      secondary: string;
      error: string;
      success: string;
  };
  fontSizes: {
      small: string;
      medium: string;
      large: string;
  }
}
